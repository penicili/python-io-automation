[project]
name = "python-io-automation"
version = "0.1.0"
description = "Belajar chore automation"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "loguru>=0.7.3",
    "pathlib>=1.0.1",
    "python-dotenv>=1.2.2",
    "pyyaml>=6.0.3",
]
